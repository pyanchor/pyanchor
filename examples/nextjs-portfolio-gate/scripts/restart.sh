#!/usr/bin/env bash
# Stub PYANCHOR_RESTART_SCRIPT for the portfolio-gate example.
# In production, replace with your actual frontend reload command
# (pm2 reload <name>, systemctl restart <unit>, fly deploy, etc.).
#
# This stub just exits 0 so pyanchor's "build → restart" step
# succeeds during local testing.
exit 0
