#!/usr/bin/env bash
# Stub PYANCHOR_RESTART_SCRIPT for the minimal example.
#
# In a real deployment, replace the body with whatever restarts your
# Next.js process (pm2 restart, docker compose restart, systemctl, ...).
#
# `next dev` already hot-reloads on file changes, so this stub is
# intentionally a no-op for the example.

set -euo pipefail
echo "[restart.sh] no-op stub; next dev hot-reloads on file changes" >&2
