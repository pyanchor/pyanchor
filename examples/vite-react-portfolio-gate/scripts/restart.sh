#!/usr/bin/env bash
# Stub PYANCHOR_RESTART_SCRIPT for the vite-react-portfolio-gate example.
# In production, replace with your actual frontend reload command.
# For local `vite dev`, the dev server hot-reloads on file changes,
# so this can stay a no-op.
exit 0
