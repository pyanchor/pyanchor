<main>
  <h1>Hello, pyanchor (SvelteKit)</h1>
  <p>
    SvelteKit isn't a built-in framework profile yet — pyanchor falls back to
    the nextjs profile and prints a warning. This example shows the explicit
    <code>PYANCHOR_INSTALL_COMMAND</code> / <code>PYANCHOR_BUILD_COMMAND</code>
    override path that lets <em>any</em> framework run.
  </p>
  <p class="hint">
    Click the floating button (bottom-right) to ask the agent to change this page.
  </p>
</main>

<style>
  main {
    text-align: center;
    padding: 2rem;
    max-width: 560px;
  }
  h1 {
    font-size: 2.4rem;
    margin: 0;
  }
  p {
    opacity: 0.75;
    line-height: 1.6;
    margin-top: 16px;
  }
  .hint {
    opacity: 0.6;
    font-size: 0.9rem;
    margin-top: 24px;
  }
  code {
    background: rgba(255, 255, 255, 0.08);
    padding: 1px 6px;
    border-radius: 4px;
    font-size: 0.9em;
  }
</style>
