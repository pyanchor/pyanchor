#!/usr/bin/env bash
# Stub PYANCHOR_RESTART_SCRIPT for the vite-react-minimal example.
#
# In a real deployment, replace the body with whatever restarts your
# Vite/Node process (pm2 restart, docker compose restart, systemctl, ...).
#
# `vite` already hot-reloads on file changes, so this stub is
# intentionally a no-op for the example.

set -euo pipefail
echo "[restart.sh] no-op stub; vite hot-reloads on file changes" >&2
